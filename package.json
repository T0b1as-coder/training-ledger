{
  "name": "training-ledger-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Dev-only Playwright end-to-end tests for Training Ledger. The shipped app (index.html) has zero runtime dependencies -- only this test tooling uses npm.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
